import { AutoInputLengthDirective } from './auto-input-length.directive';

describe('AutoInputLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoInputLengthDirective();
    expect(directive).toBeTruthy();
  });
});
